name: CLA Assistant

# Enforces that external contributors sign the CLA before their PR can merge.
# Teqpace employees are covered by their employment IP-assignment and can be
# added to `allowlist` instead of signing.
#
# SETUP (one-time, by a maintainer):
#   1. Create a Personal Access Token (repo scope) and add it as the repo secret
#      CLA_SIGNATURES_TOKEN (used to store signatures).
#   2. Add real Teqpace employee GitHub usernames to `allowlist` below.

on:
  issue_comment:
    types: [created]
  pull_request_target:
    types: [opened, closed, synchronize]

permissions:
  actions: write
  contents: write
  pull-requests: write
  statuses: write

jobs:
  cla:
    runs-on: ubuntu-latest
    steps:
      - name: CLA Assistant
        if: (github.event.comment.body == 'recheck' || github.event.comment.body == 'I have read the CLA Document and I hereby sign the CLA') || github.event_name == 'pull_request_target'
        uses: contributor-assistant/github-action@v2.6.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PERSONAL_ACCESS_TOKEN: ${{ secrets.CLA_SIGNATURES_TOKEN }}
        with:
          path-to-signatures: 'signatures/cla.json'
          path-to-document: 'https://github.com/teqpace-services/isopace/blob/main/CLA.md'
          branch: 'main'
          # TODO: add Teqpace employee GitHub usernames here.
          allowlist: 'dependabot[bot]'
